spring:
  application:
    name: identity-service
  profiles:
    default: dev
  cloud:
    config:
      enabled: false

# Common settings across profiles can go here.
management:
  endpoints:
    web:
      exposure:
        include: health,info

# Strongly recommended for stable OIDC discovery + tokens behind reverse proxies
# spring:
#   security:
#     oauth2:
#       authorizationserver:
#         issuer: https://identity.company.com